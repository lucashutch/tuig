{
  "name": "tuig",
  "version": "0.1.9",
  "description": "A mouse-first Git client for the terminal",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lucashutch/tuig.git"
  },
  "homepage": "https://github.com/lucashutch/tuig#readme",
  "bugs": "https://github.com/lucashutch/tuig/issues",
  "keywords": [
    "git",
    "terminal",
    "tui",
    "bun"
  ],
  "type": "module",
  "bin": {
    "tuig": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun test",
    "lint": "eslint ."
  },
  "dependencies": {
    "@opentui/core": "0.5.7",
    "tree-sitter-wasm": "1.1.7"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "latest",
    "eslint": "^10.9.1",
    "prettier": "3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.68.0"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}
